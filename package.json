{
  "name": "eyerise-mirror",
  "version": "1.0.0",
  "description": "SEO-optimized companion site for the EyeRise Substack publication.",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync.js",
    "build": "eleventy",
    "serve": "eleventy --serve --port=8080",
    "clean": "rm -rf public",
    "search-index": "node scripts/build-search-index.js",
    "all": "npm run sync && npm run build && npm run search-index",
    "dev": "npm run sync && npm run serve"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "rss-parser": "^3.13.0",
    "luxon": "^3.4.4",
    "slugify": "^1.6.6",
    "node-html-parser": "^6.1.13"
  },
  "engines": {
    "node": ">=18"
  }
}
